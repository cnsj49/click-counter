name: Lab

on:
  workflow_dispatch:
  push:
    branches:
      - lab

jobs:
  any_name:
    runs-on: ubuntu-latest
    steps:
      - name: Test Step
        shell: bash
        run: |
          echo "Test"